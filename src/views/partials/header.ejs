<header>
  <nav>
    <div>
      <a href="/">
        <img class="logo" src="/images/gabooklogoBlanco1.png" alt="" />
      </a>
    </div>
    <form class="search" action="/search" method="GET">
      <input
        type="search"
        name="keywords"
        placeholder="  Búsqueda..."
        id="search-bar"
      />
      <i class="fas fa-search"></i>
    </form>
    <ul class="user-cart">
      <% if (locals.isLogged ) {%>
      <li>
        <div>
          <img
            class="header-login-image"
            src="/images/users/<%= locals.userLogged[0].image%>"
            alt="ImageProfile"
          />
          <div id="header-user-menu">
            <% if (locals.userLogged[0].role == 1 ) {%>
            <a href="/admin/users"> Usuarios</a>
            <a href="/products">Productos</a>
            <%}%>
            <% if (locals.userLogged[0].role != 1 ) {%>
            <a href="/edit-profile">Perfil</a>
            <%}%>
          </div>
        </div>
      </li>
      <%}%> <% if (locals.isLogged ) {%>
      <li><p class="header-user-log"><%= locals.userLogged[0].name%></p>
      </li>
      <%}%>
      <li>
        <a href="/cart"><i class="fas fa-shopping-cart"></i></a>
      </li>
      <% if (locals.isLogged ) {%>
      <li>
        <a href="/logout"><i class="fas fa-sign-out-alt"></i></a>
      </li>
      <%}%> <% if (!locals.isLogged ) {%>
      <li>
        <a href="/signIn"><i class="fas fa-user-plus"></i></a>
      </li>
      <%}%> <% if (!locals.isLogged ) {%>
      <li>
        <a href="/login"><i class="fas fa-user-circle"></i></a>
      </li>
      <%}%>

      <!-- <li>
        <i class="fas fa-bars"></i>
      </li> -->
    </ul>
  </nav>
</header>
